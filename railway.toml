[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 wsgi:application"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
